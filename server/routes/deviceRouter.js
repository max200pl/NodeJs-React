const Router = require("express")
const router = new Router()

router.post("/",) //  
router.get("/",) // 
router.get("/:id",) //для того что бы получить отельно взятый девайс после того как мы перешли на страницу подробной информации 



module.exports = router